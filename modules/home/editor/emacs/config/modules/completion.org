* Completion

** Minibuffer completion

#+begin_src emacs-lisp
(use-package vertico
  :init
  (setq vertico-resize 'grow-only)
  (setq vertico-cycle t)
  :config
  (vertico-mode 1))

(use-package orderless
  :init
  (setq completion-styles '(orderless basic))
  (setq completion-category-defaults nil)
  (setq completion-category-overrides '((file (styles basic partial-completion)))))

(use-package marginalia
  :after vertico
  :config
  (marginalia-mode 1))

(use-package consult
  :bind (("C-s" . consult-line)
         ("C-x b" . consult-buffer)
         ("C-x p b" . consult-project-buffer))
  :config
  (setq consult-project-function
        (lambda (_)
          (when-let ((project (project-current)))
            (project-root project))))
  (consult-customize
   consult-ripgrep consult-grep consult-git-grep
   :preview-key '(:debounce 0.3 any)))

(use-package embark
  :bind (("C-." . embark-act)
         ("C-;" . embark-dwim)
         ("C-h B" . embark-bindings))
  :init
  (setq prefix-help-command #'embark-prefix-help-command))

(use-package embark-consult
  :after (embark consult)
  :hook (embark-collect-mode . consult-preview-at-point-mode))

(use-package consult-dir
  :after consult
  :bind (("C-x C-d" . consult-dir)
         :map vertico-map
         ("C-x C-d" . consult-dir)
         ("C-x C-j" . consult-dir-jump-file)))
#+end_src

** In-buffer completion

#+begin_src emacs-lisp
(use-package corfu
  :init
  (setq corfu-auto t)
  (setq corfu-auto-delay 0.14)
  (setq corfu-auto-prefix 2)
  (setq corfu-preview-current nil)
  (setq corfu-cycle t)
  :config
  (global-corfu-mode 1))

(use-package cape
  :after corfu
  :init
  (add-to-list 'completion-at-point-functions #'cape-file)
  (add-to-list 'completion-at-point-functions #'cape-dabbrev))

(use-package tempel
  :commands (tempel-complete tempel-expand))

(use-package tempel-collection
  :after tempel)

(defun dotfiles/consult-ripgrep-in-directory ()
  "Run consult-ripgrep in a user-selected directory."
  (interactive)
  (let ((dir (read-directory-name "Search in directory: ")))
    (consult-ripgrep dir)))

(defun dotfiles/find-file-in-config ()
  "Find file in the dotfiles config repo."
  (interactive)
  (let ((default-directory "~/dev/personal/repos/config/"))
    (call-interactively #'find-file)))

(with-eval-after-load 'general
  (dotfiles/leader-def
    "b i" '(consult-buffer :which-key "buffer switcher")
    "f c" '(:ignore t :which-key "config")
    "f c p" '(dotfiles/find-file-in-config :which-key "personal config")
    "f f" '(find-file :which-key "find file")
    "f h" '(consult-history :which-key "history")
    "f r" '(consult-recent-file :which-key "recent files")
    "s s" '(consult-line :which-key "search line")
    "s p" '(consult-ripgrep :which-key "ripgrep project")
    "s d" '(dotfiles/consult-ripgrep-in-directory :which-key "ripgrep directory")
    "s i" '(consult-imenu :which-key "imenu")
    "s o" '(consult-outline :which-key "outline")
    "s b" '(consult-line-multi :which-key "search all buffers")
    "i s" '(tempel-insert :which-key "snippet")
    "i e" '(insert-char :which-key "emoji / char")))
#+end_src
