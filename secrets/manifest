# <scope>        <local-path>              <op-uri>
# Scopes: shared | work | host:<name>
#
# Secrets are pulled to $PLATFORM_SECRETS_DIR/<local-path>
# SSH private keys are NOT listed here — use 1Password SSH agent instead.

shared            git/config                op://Private/git-config/notesPlain
shared            git/allowed_signers       op://Private/git-allowed-signers/notesPlain
